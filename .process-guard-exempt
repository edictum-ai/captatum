# Engineering OS — stage-artifact exemption marker.
#
# This repo predates the acceptance-suite pipeline. While this file exists, the
# process-guard `stage-artifact` check stays quiet (src/** may change without a
# frozen acceptance manifest on the base branch). The freeze-hash and mixed-diff
# checks still run.
#
# This exemption is VISIBLE, never silent: it is listed as a named gap in every
# monthly audit. It comes out when the first trust-boundary change lands its
# frozen acceptance suite (see engineering-os/ONBOARDING.md "The ratchet").
